# Security
requirepass ${REDIS_PASSWORD}
protected-mode yes
bind 0.0.0.0

# Persistence
dir /data
appendonly yes

# Memory management
maxmemory 256mb
maxmemory-policy allkeys-lru

# Network
tcp-keepalive 300
timeout 300
